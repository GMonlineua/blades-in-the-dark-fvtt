<form class="{{cssClass}} {{item.type}}" autocomplete="off">
  {{!-- Sheet Header --}}
  <header class="items-header">

    <img class="item-img" src="{{item.img}}" data-edit="img" title="{{item.name}}"/>

    <div class="header-fields">
      <h1 class="item-name"><input name="name" type="text" value="{{item.name}}" placeholder="{{localize 'BITD.ItemName'}}"/></h1>
      <div class="brief-info">
        <select class="prop clear-select" name="system.type">
          {{selectOptions config.cohort.types selected=system.type localize=true}}
        </select>
        <div class="prop">
          <label class="prop-label" for="system.elite">{{localize 'BITD.Cohort.Elite'}}</label>
          <input class="prop-input" type="checkbox" name="system.elite" data-dtype="Boolean" {{checked system.elite}}></input>
        </div>
        <div class="prop">
          <label class="prop-label" for="system.armor">{{localize 'BITD.Cohort.Armor'}}</label>
          <input class="prop-input" type="checkbox" name="system.armor" data-dtype="Boolean" {{checked system.armor}}></input>
        </div>
      </div>
    </div>

  </header>

  {{!-- Sheet Body --}}
  <section class="sheet-body">
    <hr>
    <div class="harm">
      <label>{{localize 'BITD.Harm'}}:</label>
      <select class="clear-select" name="system.harm">
        {{selectOptions config.cohort.harm selected=system.harm localize=true}}
      </select>
    </div>

    <div class="edges">
      <h3>{{localize 'BITD.Cohort.Edges'}}:</h3>
      <input class="all-with" name="system.edges" type="text" value="{{system.edges}}" placeholder="{{localize 'BITD.Cohort.Edges'}}" data-dtype="String"></input>
    </div>

    <div class="flaws">
      <h3>{{localize 'BITD.Cohort.Flaws'}}:</h3>
      <input class="all-with" name="system.flaws" type="text" value="{{system.flaws}}" placeholder="{{localize 'BITD.Cohort.Flaws'}}" data-dtype="String"></input>
    </div>

    <h3>{{localize 'BITD.Description'}}</h3>
    {{editor enrichedDescription target="system.description" rollData=rollData button=true owner=owner editable=editable}}
  </section>
</form>
